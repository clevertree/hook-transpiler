{
  "name": "@clevertree/hook-transpiler",
  "version": "1.3.3",
  "description": "Minimal JSX/TSX transpiler for Relay hooks on web and Android (QuickJS/JNI)",
  "type": "module",
  "main": "./dist/web/index.js",
  "types": "./dist/web/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/web/index.d.ts",
      "import": "./dist/web/index.js",
      "default": "./dist/web/index.js"
    },
    "./android": {
      "types": "./dist/android/index.android.d.ts",
      "import": "./dist/android/index.android.js",
      "default": "./dist/android/index.android.js"
    }
  },
  "files": [
    "dist",
    "android",
    "wasm"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc && npm run build:wasm",
    "build:wasm": "wasm-pack build --release --target web --features wasm && mkdir -p wasm && cp pkg/relay_hook_transpiler* wasm/ && mkdir -p dist/web/wasm && cp pkg/relay_hook_transpiler* dist/web/wasm/ && mkdir -p dist/wasm && cp pkg/relay_hook_transpiler* dist/wasm/",
    "test": "npm run build && node --test dist/selfCheck.test.js",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^18.0.0",
    "express": "^4.18.2",
    "react": "^18.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "markdown-to-jsx": "^9.3.0"
  },
  "peerDependencies": {
    "react": ">=18.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "publishConfig": {
    "access": "public"
  },
  "sideEffects": false
}